#!/bin/bash

# Cleanup for packages and orphaned dependencies using pacman and yay.
echo "Removing orphaned packages..."
sudo pacman -Rns $(pacman -Qtdq)
echo "Cleaning up yay cache..."
yay -Sc
terminal-show-done